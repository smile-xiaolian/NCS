# NCS × my_device_link_sim 联动演示指南

两个项目的联动关系一句话概括：**NCS 主工程管业务（人、单、数据），my_device_link_sim 管设备通信（桩、连接），通过共享 SQLite 数据库 `charge_platform.db` 桥接**。

- 状态上行（桩 → 管理端）：桩 → WebSocket → 模拟器平台 → 写数据库 → 管理端刷新可见
- 指令下行（管理端 → 桩）：管理端「远程重启」→ HTTP `/reset` → 模拟器平台 → WebSocket 下发 `RemoteReset` → 桩真正复位

## 一、编译

```bash
cd ~/projects/NCS

# 主工程（ncs_user / ncs_admin）
cmake --build build-linux -j

# 模拟器（platform_app / pile_app，首次需 configure）
cmake -S my_device_link_sim -B my_device_link_sim/build
cmake --build my_device_link_sim/build -j
```

依赖（Ubuntu，缺什么装什么）：

```bash
sudo apt install build-essential cmake qt6-base-dev qt6-charts-dev \
     qt6-webengine-dev qt6-multimedia-dev libqt6sql6-sqlite libxkbcommon-dev
```

## 二、启动程序（顺序很重要）

**必须先启动管理端**——它要完成数据库建表/迁移，模拟器平台随后才能读到桩清单。
数据库位置：`~/.local/share/NCS/charge_platform.db`。

| 顺序 | 终端 | 命令 | 说明 |
|---|---|---|---|
| 1 | 终端1 | `./build-linux/bin/ncs_admin` | 管理端，登录 `admin / 123456`，确认「充电桩管理」里有桩（如 S1-01） |
| 2 | 终端2 | `./my_device_link_sim/build/platform_app/platform_app --port 9000 --demo` | 监控平台，`--demo` 自动把数据库在册桩全部接入为内置虚拟桩 |
| 3 | 终端3 | `./build-linux/bin/ncs_user` | 车主端（可选，演示用户视角） |

**不需要单独启动 pile_app**：平台界面每张桩卡片有「打开桩窗口」按钮，点击即可在平台进程内弹出该桩的桩端界面（故障注入、断线等操作都在里面），与平台双向实时联动。

> ⚠️ 不要再启动 `pile_app --code S1-01` 之类与内置演示桩同编号的外部进程——同编号连接会互相顶替，导致"连接→断开→重连"无限循环（编号不在数据库里的如 `PILE-001` 则无冲突，可用于纯通信演示）。

> 启动顺序若颠倒或同时启动，管理端可能报"数据库初始化失败"（写锁冲突）。正常情况下 5 秒 busy_timeout 会自动重试；若仍报错，先关掉所有相关进程再按顺序启动。

## 三、联动演示流程

### 演示 1：桩故障 → 管理端发现（状态上行）

1. 监控平台上点 S1-01 卡片的「打开桩窗口」，弹出桩端界面
2. 桩窗口点「**模拟故障**」——桩状态机转 Faulted，经 WebSocket 实时上报
   - 平台卡片立刻变「故障」状态灯
   - 平台回写数据库 `charger.status = 2`，`ops_log` 追加一条"故障"
3. 管理端切一下页签（或点「刷新」）——S1-01 显示故障

> 管理端是**拉取模式**：没有定时器、没有推送，只在切页签/点刷新/操作后三种时机重新查库。实时看到故障的是监控平台（它持有 WebSocket 连接）。

### 演示 2：远程重启 → 桩真正复位（指令下行，本次联动核心）

前提：桩处于**故障**状态（正在充电的桩管理端会拒绝重启）。

1. 管理端「充电桩管理」选中故障桩，点「**远程重启**」并确认
2. 同时发生两条线：
   - 原有逻辑：数据库 `status = 0` + `ops_log` 记"远程重启并恢复正常" + 弹成功提示
   - 联动挂钩：HTTP 调用 `http://127.0.0.1:9080/reset?pileCode=S1-01` → 平台向该桩下发 `RemoteReset` → **桩窗口故障消失、复位为空闲**
3. 验证点：平台日志出现 RemoteReset 记录；桩窗口/卡片复位；管理端刷新后状态空闲

### 演示 3：降级验证（挂钩不添乱）

1. 关闭监控平台（终端2 Ctrl+C）
2. 管理端再点一次「远程重启」
3. 预期：行为与模拟器存在时完全一样（弹成功、状态置空闲、写日志），不卡、不报错；管理端终端只多一行 `[device_link] 模拟器未响应(可忽略)` 调试输出

### 演示 4：断线与自愈（模拟器自身能力）

1. 桩窗口点「**手动断线**」（模拟网络断开，区别于"模拟故障"=设备坏了）
2. 看平台：心跳超时，30 秒内卡片灰显"离线"
3. 桩按指数退避自动重连（1s→2s→4s→…封顶 30s），重新上线亮灯
4. 想让桩"真死"不回来：直接关掉桩窗口或杀 pile_app 进程

### 可选：挂钩开关配置

在 `build-linux/bin/` 下放 `config.ini` 可控制下行挂钩（无文件时默认启用）：

```ini
[device_link]
hook_enabled=true
reset_url=http://127.0.0.1:9080/reset
```

## 四、故障注入按钮对照表

| 按钮 | 模拟什么 | 连接状态 | 平台表现 | 数据库变化 | 管理端动作 |
|---|---|---|---|---|---|
| 模拟故障 | 设备坏了 | 保持连接 | 立刻显示"故障" | `status=2` + ops_log | **远程重启**（正是为此设计） |
| 手动断线 | 网络断了 | 断开 | 30 秒内判"离线"灰显 | 无变化（离线≠故障） | 无（联系不上设备） |
| 恢复正常 | 故障修复 | 保持连接 | 立刻恢复"空闲" | 回写状态 | — |
| 模拟充满 | 充电完成 | 保持连接 | 结束充电 | 记录结束 | — |

## 五、实现位置速查

| 联动环节 | 代码位置 |
|---|---|
| 状态回写（桩→库） | `my_device_link_sim/platform_app/ProjectPileDb.cpp`（setStatus / appendOpsLog） |
| HTTP 触发口 | `my_device_link_sim/platform_app/TriggerHttpServer.cpp`（GET /reset → remoteReset） |
| 下行挂钩（管理端→HTTP） | `client_admin/ui/DeviceLinkHook.cpp`（异步 GET，2s 超时，失败静默） |
| 重启按钮接入 | `client_admin/ui/ChargerManagePage.cpp` `restartCharger()` |
| 挂钩配置 | `client_admin/ui/DeviceLinkHook.cpp` `hookConfig()`（读 config.ini） |
| 并发写库保护 | `core/database/DatabaseManager.cpp`（`PRAGMA busy_timeout = 5000`） |
